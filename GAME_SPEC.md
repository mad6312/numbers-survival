# ナンバーズ・サバイバル 正式仕様書 (MASTER)
※本ファイルはゲーム実装・改修の基準となる公式仕様書（Single Source of Truth）です。
※AIおよび開発者は、本仕様書に記述されたルールおよび設計を最優先として実装・機能追加を行ってください。

---

## 1. ゲーム内テキスト（文言）の統一と管理規約

### ■ 適用範囲
本規約は、ゲーム内でプレイヤーに対して表示されるすべてのテキスト表現および文言に適用される。
1. 各種モーダル、ダイアログ上の見出し、説明文、ボタンテキスト
2. その他、ゲームプレイ中に画面上に表示されるすべてのシステムメッセージ・表記

### ■ 文言の策定・管理主体
* ゲーム内に表示されるすべての文言は、**仕様策定者（担当者）が直接記述・管理**を行う。

### ■ 変更・改変に関する禁止事項
1. **無断変更・アレンジの禁止**:
   明確な指示または事前の協議がない限り、プログラム実装時およびUIデザイン作成時において、テキスト文言・ニュアンス・言い回しを開発者側で勝手に変更・要約・アレンジすることを禁止する。
2. **システム制約時の事前確認義務**:
   レイアウト崩れや文字数オーバー等のシステム上の制約により、どうしても文言の変更・短縮が必要となる場合は、開発者が独自に判断して変更せず、**必ず事前に仕様策定者へ確認・提案を行い、了承を得た上で修正**を行うこと。

---

## 2. ゲーム概要 ＆ 基本設定

* **ゲームタイトル**: ナンバーズ・サバイバル (Numbers Survival)
* **ゲームジャンル**: オンライン対戦型 数字選択心理サバイバルゲーム
* **プレイ人数**: 2〜10名
* **初期ライフ**: 各プレイヤー 3（表示: ♥♥♥）
* **総使用数字（初期カードプール）**: `(初期参加人数 - 1) × 12`
  * 例: 2人プレイ ➔ 1〜12 の数字を使用
  * 例: 4人プレイ ➔ 1〜36 の数字を使用
  * 例: 10人プレイ ➔ 1〜108 の数字を使用
* **行動順**: ゲーム開始時に全参加者の中からランダムで決定（ゲーム終了まで固定）。

---

## 3. ラウンド進行と役割（キラー / サバイバー）

### 3-1. キラー選出ロジック
* 各ラウンド開始時、**生存者の中から1名をランダムでキラーに選出**（残りはサバイバー）。
* **均等循環システム**: 生存者全員が1回ずつキラーを担当するまで、同じプレイヤーが再度キラーに選ばれることはない（全生存者が担当した時点で履歴リセット）。
* ※途中で脱落（ライフ0）したプレイヤーは、次回以降のキラー選出ルーレットおよび循環履歴から除外される。

### 3-2. ラウンドの流れ

#### フェーズⅠ. キラーの罠設置
1. キラーは残っている数字プールの中から **`(初期参加人数 - 1)` 個** の数字をクリックして選択し、罠を設置する。
   * ※途中で脱落者が出た場合でも、設置する罠の個数は初期参加人数基準のまま変動しない。
2. 設置された罠の数字は、キラー本人にのみ可視化され、**サバイバー側には完全非公開（隠蔽）**とされる。

#### フェーズⅡ. サバイバーの数字選択
1. サバイバーは行動順に従い、残っている数字プールから **1人1個ずつ** 数字を選択する。
2. **重複選択不可**: 同ラウンド内ですでに他のサバイバーが選択した数字は選択できない。
3. **手番強調表示**: 現在数字を選択しているサバイバーのプレイヤー枠には、シアン/ブルーのパルス強調枠（`.is-active-turn`）が表示され、全員に手番が一目でわかるよう明示される。

#### フェーズⅢ. 判定・演出
1. 全サバイバーの選択完了後、即座に判定フェーズへ移行。
2. **カウントダウン演出**:
   * 中央に「3, 2, 1」が1秒間隔でポップアップ表示される。
   * 演出発生中も**画面ぼかし（ブラー）は行わず透過オーバーレイを適用**し、盤面中央の数字カードや全プレイヤーのステータス情報が常時くっきり視認可能な状態を維持する。
3. **結果演出**:
   * **回避成功（Safe）**:
     * 強調表示: 「Safe」 ＋ 「+〇〇 pts 獲得！」
     * 効果: 選択した数字と同値のポイントを獲得。選択されたSafeの数字はプールから完全に除外（消費）される。
     * 枠色変化: 該当サバイバーのプレイヤー枠を **青色（Safeカラー）** に変更。
   * **罠接触（Out）**:
     * 強調表示: 「Out」 ＋ 「ポイント全額没収 & ライフ消費！」
     * 効果: 該当プレイヤーの所持ポイントを全額没収（0pt）し、ライフを1消費（例: ♥♥♥ ➔ ♥♥♡）。ライフが0になったプレイヤーは脱落（dead状態）となる。
     * 枠色変化: 該当サバイバーのプレイヤー枠を **赤色（Outカラー）** に変更。
   * ※枠色の変更エフェクトは結果表示演出終了後（次ラウンド開始時）に通常色へ自動リセットされる。

#### フェーズⅣ. ラウンド終了
* キラー権を次の未担当生存者へ交代し、次ラウンドへ遷移する。

---

## 4. ゲーム終了（勝利）条件

以下のいずれかの条件を満たした時点でゲームは即座に終了し、最終結果画面へ遷移する。

1. **目標ポイント到達**:
   * いずれかのプレイヤーが **`(初期参加人数 - 1) × 40` ポイント以上** を獲得した時点（例: 4人プレイ時 ➔ 120pt）。
   * 同時に複数名が到達した場合は、最高ポイント保持者が勝者。
2. **生存者1名（他プレイヤーの全滅）**:
   * ライフが残っているプレイヤーが最後の1人になった時点で、そのプレイヤーが生存勝利。
3. **数字プールの枯渇**:
   * 残り数字の個数が **`(初期参加人数 - 1)` 個以下** になった時点。
   * その時点で最もポイントが高いプレイヤーが勝者。

---

## 5. ロビー・エントリー処理

### 5-1. エントリー機能
* 「参加する」ボタンでエントリー完了。トグル仕様となっており、再度押下で「参加キャンセル」が可能。
* ゲーム終了後や途中退出時を含め、ボタンは常時確実に反応するようステートが管理される。

### 5-2. リアルタイム同期
* ロビー画面上に「現在の参加人数（例: 2 / 4）」および「参加中... / 待機中」バッジが全接続クライアントへリアルタイム同期される。

### 5-3. プレイヤー設定モーダル
* エントリー前後・ゲーム中を問わず、画面右上の「⚙️ 設定」ボタンからいつでも変更可能。
* **プレイヤー名**: 最大10文字まで入力可能。
* **アバター画像**: システム側で用意されたプリセット（ロボット・動物など全10種）からワンクリックで選択可能。

### 5-4. ゲーム開始条件
* 部屋に存在する「ホスト（部屋主）」にのみ「ゲーム開始」ボタンが表示される。
* エントリー完了者（参加中）が **2名〜10名** の範囲にある場合のみボタンが活性化し、押下によって即座に全プレイヤーがゲーム画面へ遷移する。

---

## 6. メインゲーム画面（UI・盤面レイアウト設計）

### 6-1. 自陣（自分）情報エリア
* 画面手前中央（下部）に常時固定表示。
* 表示項目: 「アバター画像」「プレイヤー名」「役割バッジ（キラー / サバイバー / あなたの番！）」「現在のライフ（♥♥♥形式）」「現在の獲得ポイント」。

### 6-2. 対戦相手情報エリア
* 画面上部に並列配置（複数人の場合は中央揃えラップ表示）。
* 表示項目: 「アバター画像」「プレイヤー名」「現在のライフ」「現在の獲得ポイント」。
* **役割表示**:
  * 現ラウンドのキラー: 紫色の不気味なパルス枠線エフェクト（`.is-killer`）および「キラー」バッジを表示。
  * 手番サバイバー: シアン色のパルス枠線エフェクト（`.is-active-turn`）および「選択中」バッジを表示。
  * 脱落者: 半透明グレーアウト（`.dead`）表示。

### 6-3. 盤面中央（数字カードプール領域）
* 画面中央に数字カードを均等・中央揃え（Flex均等配置）でバランスよく整列表示。
* **消費済み数字**: 過去ラウンドで除外されたSafe数字は削除せず、ダークグレー（非活性）表示で盤面に残す。
* **選択状態の可視化**:
  * サバイバーが数字を選択した際、選択された数字カードの右上に該当プレイヤーのアバター画像バッジを重複配置。
  * **ホバーツールチップ機能**: アバター画像バッジにマウスオーバーすることで、該当プレイヤーの登録名（プレイヤー名）がポップアップ表示される。
* **キラー専用UI**: キラーには指定個数分の選択枠および「罠を設置する (現在数/必要数)」のアクションバーが中央上部に表示される。

---

## 7. 最終対戦結果画面 ＆ 退室処理

### 7-1. 結果表示
* 終了理由（目標ポイント到達 / 生存者1名 / プール枯渇）を明記。
* 全参加者を最終順位、アバター、プレイヤー名、最終ライフ、獲得ポイントの順位テーブルで一覧表示。

### 7-2. 再戦 ＆ 退出ボタン
結果モーダル下部に「再戦」および「退出する」ボタンを配置。

* **「再戦」ボタン**:
  * 押下時、サーバーへエントリー要求を送信。
  * 結果画面を閉じ、ロビー待機状態（「参加中...」バッジ有効状態）へ遷移。
* **「退出する」ボタン**:
  * 押下時、ルームのエントリーリストから除外。
  * 結果画面を閉じ、ロビーの初期状態（「参加する」ボタン表示の未エントリー待機状態）へ遷移。

### 7-3. 【重要：不具合防止要件】個別ソケット単位の完全独立管理
* 1人が「再戦」や「退出」を押下した際、**他プレイヤーのステートに自動的に影響を与えてはならない**。
* 必ずSocket ID単位で個別に従属ステートを処理し、他プレイヤーが勝手に再戦エントリーされたり退出扱いになる不具合を完全に防止する設計を維持すること。